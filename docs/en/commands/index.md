# Commands

`indexnow` currently exposes a single subcommand:

- **[`indexnow submit`](submit.md)** — submit one or more URLs to an IndexNow endpoint.

Run `indexnow --help` or `indexnow submit --help` for the full flag set.
